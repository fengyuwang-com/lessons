# Docker 容器内文件不持久化

## 日期
2026-07-02

## 问题
在运行中的容器里删除了文件，重建容器后文件又出现了。

## 原因
容器的文件系统是分层的：
- 运行时的修改在最上层（可写层）
- 重建容器时，可写层被丢弃，从镜像层重新创建
- 所以删除操作在重建后"失效"了

## 正确做法
要持久化修改，必须用 `-v` 挂载外部文件：

```bash
docker run -d \
  -v "/host/path/config.yml:/app/config.yml" \
  moonbridge:local
```

或者修改 Dockerfile 重新构建镜像。

## 教训
容器内的修改都是临时的。需要持久化的配置必须挂载卷或重建镜像。
