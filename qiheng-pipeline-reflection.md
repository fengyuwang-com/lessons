# Qiheng B站转写管线 — 项目复盘

## 2026-07-18 第二期工作

---

## 问题 1：目录整理时丢失中间文件

**现象：** 重组项目目录结构时，删除了 `bilibili_transcripts/` 中 204 个 Whisper 原始转写文件（`.txt`）。认为"有 `_corrected.txt` 就够了"。

**根因：** 没有认识到每个阶段的文件都有独立价值，原始转写是排查 Whisper 转写问题的唯一依据。

**教训：**
- 管线每一步的中间产物都有其不可替代的用途
- 涉及文件删除/移动的操作，必须先列出完整的影响范围，经确认后再执行
- 三个层级独立保存：源音频 → 原始转写 → 校正版

**修复：** 好在音频还在，通过重新 Groq 转写挽回。但浪费了时间。以后文件清理前必须做 impact analysis。

---

## 问题 2：用文件大小估算音频时长，反复改公式

**现象：** 先假设 128kbps → 实际 40kbps → 实际 VBR 37-53kbps → 直到最后才用 ffprobe 直接读。

**公式演化：**
```
① bytes/16000 (128kbps) → 全错，453 个都判断为"不完整"
② bytes/5000 (40kbps)  → 还是不对，显示 120%
③ bytes/5813 (46.5kbps) → 仍不准，VBR 没有固定值
④ ffprobe 直接读       → 正确 ✅
```

**根因：** 想"估算"而不是"直接读取"，ffprobe 一直可用但没第一时间想到。

**教训：**
- 有现成的工具（ffprobe）就直接用，不要自己算
- 花在一个错误假设上的调试时间，远超"先查有没有现成工具"的时间
- 以后再遇到"这个文件的 X 属性是多少"的问题，先找读取工具，不估算

---

## 问题 3：目录层级过多

**现象：** 创建了 `00_raw/audio/`、`01_intermediate/transcripts/`、`02_output/corrected/` 三层编号结构。

**导致：** 用户看不明白，最终又拆掉重来。

**根因：** 过度设计。用户只需要扁平清晰的分类——源文件、工具、输出。

**教训：**
- 目录结构不要超过两层
- 命名用自然语言（`audio/`、`output_bilibili/`），不用编号
- 先跟用户对齐结构再落地

---

## 问题 4：文件"完整性"定义不完整

**现象：** 将"音频完整性 ≥90%"等同于"文件完整"。没有验证转写质量和校正质量。

**根因：** 只验证了下游（音频时长），没验证上游（转写内容量、校正是否合理）。

**教训：**
- "完整"的定义应该分层：音频完整 → 转写完整 → 校正完整
- 每层有各自的检查方法：
  - 音频：ffprobe 时长 vs B站 API 时长
  - 转写：中文数量 vs 音频时长（正常语速 ~200字/分钟）
  - 校正：结尾是否自然（"今天就讲到这里" vs 戛然而止）

---

## 问题 5：chunk 切太小导致 API 不可靠

**现象：** 设 chunk_size=2000，导致 58K 的文本切成 29 段。29 次 API 调用任一个失败就会前功尽弃。

**根因：** 没考虑到 API 调用的失败概率与调用次数成正比。

**教训：**
- 每次外部 API 调用都有失败概率（429、超时、断连）
- 调用次数越多，整体成功概率越低
- chunk 大小应该在 4000-5000 字，而不是 2000 字
- 宁可一次处理不完（截断），也不要 30 次小调用（大概率失败）

---

## 问题 6：没有及时报告状态

**现象：** 目录整理、文件删除、公式变换等操作都没有提前报告。

**根因：** 习惯性地"先做再说"，而不是"先报再做"。

**教训：**
- 涉及文件改动的操作，必须先报告影响范围
- 推测/估算型的决策，先说明局限性再动手
- 每次阶段性进展，主动汇报成果和异常

---

## 改进规则（已写入 CLAUDE.md 和管线文档）

1. **保留中间产物** — 原始音频、原始转写、校正版三者独立保存
2. **先报告再操作** — 涉及文件改动必须列影响范围
3. **用现成工具** — 不估算，ffprobe 直接读
4. **目录扁平** — 不超过两层，自然语言命名
5. **定义清晰** — 每个"完整"标准分层定义
6. **控制 API 调用量** — chunk 4000-5000 字，不切碎
