# 未验证配置是否真正生效

## 日期
2026-07-02

## 问题
改完配置后只测试了"能不能通"，没有确认实际使用的是新配置还是旧配置。

## 原因
测试返回了正常结果，就以为成功了。实际上请求还是走的旧 provider。

## 正确做法
改完配置后必须检查日志，确认 provider 名称：

```bash
docker logs moonbridge --tail 5
# 要看到 provider=你的新配置名，不是 provider=deepseek
```

## 教训
功能测试通过不代表配置正确。要验证"走的是哪条路径"，不只是"能不能用"。
